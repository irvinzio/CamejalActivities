/**
 * CargosController
 *
 * @description :: Server-side logic for managing cargos
 * @help        :: See http://sailsjs.org/#!/documentation/concepts/Controllers
 */
//var ObjectID = require('sails-mongo/node_modules/mongodb').ObjectID;

module.exports = {
/*
    update: function(req, res){
    	//params = req.params.all();
        //objectId = new ObjectID(req.param('id'));

        Cargos.update({id:req.param('id')},{tipo:req.param('tipo')}).exec(function afterwards(err, updated){
            console.log("esta haciendo n pedo");
          if (err) {
            console.log('Some error occured ' + err);
            return res.json(500, { error: 'Some error occured' });
          }
          return res.json(200, { success: 'Success' });
          console.log('Record updated' + updated);
        });
    }
    */
    /* 
    find: function (req, res) {

			var id=req.param("id",null);

			Cargos.findOne(id).done(function(err,model){

				//res.render( ‘user/view’,{‘model’:model});
				return res.json(200, { success: 'Success' });

			});

		}
		 */
};
